namespace ClinicSystem.UI.ViewModels;

/// <summary>
/// Shared form state enum used across all registry ViewModels
/// (Patients, Users, Products, etc.).
/// </summary>
public enum FormMode
{
    View,
    Add,
    Edit,
    Details
}
